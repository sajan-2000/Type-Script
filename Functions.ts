// function greet(name: string, id: number): string {
//     return `Hii ${name} Welcome your id is ${id}`;
// }

// console.log(greet("Sajan", 5));

const greet = (name: string, id: number): string => {
    return `Hii ${name} Welcome your id is ${id}`;
}

console.log(greet("Sajan", 5));
